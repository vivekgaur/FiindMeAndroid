<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MerchantConfirmDealActivity" >

    <TextView
        android:id="@+id/m_confirm_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#dc6800"
        android:textSize="16sp"
        android:textStyle="bold"
        android:text="@string/congratulation" />
    
    <Button
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignRight="@+id/confirm_label"
	    android:layout_below="@+id/m_confirm_label"
	    android:layout_margin="50dp"
	    android:layout_marginTop="122dp"
	    android:id="@+id/m_back_button"
	    android:text="@string/m_button_back" />

</RelativeLayout>
